<tr>
  <td class="userpost">
    <span class="content"><%= userpost.content %></span>
    <span class="timestamp">
      Posted <%= time_ago_in_words(userpost.created_at) %> ago.
    </span>
  </td>
  <% if current_user?(userpost.user) %>
  <td>
    <%= link_to "delete", userpost, :method => :delete,
                                     :confirm => "You sure?",
                                     :title => userpost.content %>
  </td>
  <% end %>
</tr>